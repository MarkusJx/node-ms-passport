{
  "name": "node-ms-passport",
  "version": "0.0.3",
  "description": "Microsoft passport with windows hello and credential manager for node js",
  "repository": "https://github.com/MarkusJx/node-ms-passport",
  "gypfile": true,
  "scripts": {
    "build": "cmake-js build --arch ia32 -d src --out build32 && cmake-js build --arch x64 -d src --out build64",
    "prebuild": "node prebuild.js",
    "postbuild": "node copy-deps.js copy",
    "install": "node install.js",
    "prepare": "npm run-script build",
    "create-lib": "node copy-deps.js create-lib"
  },
  "keywords": [],
  "author": "MarkusJx",
  "license": "MIT",
  "devDependencies": {
    "cmake-js": "^6.1.0",
    "mocha": "^8.0.1"
  },
  "dependencies": {
    "node-addon-api": "^3.0.0"
  }
}
